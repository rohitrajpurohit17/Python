# Check the type of variable assigned using input () function

a = input()

print(type(a))

# Doesn't matter whatever you write it gonna show string type 
# because input always taken as String
# can change it through type conversion
